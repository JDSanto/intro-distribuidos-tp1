from lib.client import transfer_file
from lib.utils import parse_download_file   
import logging

if __name__ == "__main__":
    
    args = parse_download_file()
    logging.basicConfig(level=args.verbose)
    logger = logging.getLogger(__name__)

    logger.info('Starting File Transfer App with TCP')
    transfer_file(os.path.join(args.src, args.filename), args.port, logger)
    logger.info('File transfer complete')

    